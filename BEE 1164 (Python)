ctd = 0
n = int(input())
for ind in range(n):
  x = int(input())
  for ind in range(1,x):
    if x % ind == 0:
      ctd += ind
  if ctd == x:
    print(f'{x} eh perfeito')
  elif ctd != x:
    print(f'{x} nao eh perfeito')
  ctd = 0
